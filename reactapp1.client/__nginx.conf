# # Nginx configuration for serving Vite React app
# server {
#     listen 80;
#     server_name _;
#     root /usr/share/nginx/html;
#     index index.html;

#     location / {
#         try_files $uri $uri/ /index.html;
#     }
    
#     # Proxy /weatherforecast to backend server defined by BACKEND_SERVER env variable
#     location /weatherforecast {
#         proxy_pass $backend_server;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# # Set environment variable for backend server
# env BACKEND_SERVER;
# map $BACKEND_SERVER $backend_server {
#     default $BACKEND_SERVER;
# }
